name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 或是 master，看你的主分支叫什麼

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install   <-- 改成這樣，讓系統自動產生依賴關係
          npm run build
        env:
          # 這裡會自動讀取你在 Settings > Secrets 設定的 Key
          # 對應你的 vite.config.ts 設定
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} 

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 預設打包輸出的資料夾
          branch: gh-pages # 將打包結果推送到這個孤兒分支
